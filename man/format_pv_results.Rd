% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clear_result.R
\name{format_pv_results}
\alias{format_pv_results}
\title{Format Pharmacovigilance Signal Detection Results}
\usage{
format_pv_results(
  pv_data,
  term_col = "soc_name",
  methods = NULL,
  show_significant_only = FALSE,
  clear_format = FALSE,
  significance_criteria = NULL,
  min_cases = 0,
  sort_by = "ror",
  descending = TRUE,
  max_rows = NULL,
  drop_na = TRUE,
  remove_zero_cases = TRUE
)
}
\arguments{
\item{pv_data}{A data frame containing pharmacovigilance signal detection results.}

\item{term_col}{Character. Column name for medical terminology (e.g., "soc_name", "pt_name").
Default is "soc_name".}

\item{methods}{Character vector. Signal detection methods to include. Options include:
"ror", "prr", "bcpnn_norm", "bcpnn_mcmc", "oe_ratio", "odds_ratio", "ebgm".
Default is NULL (include all available methods).}

\item{show_significant_only}{Logical. Show only statistically significant signals?
Default is FALSE.}

\item{clear_format}{Logical. Use clear format with standardized column names?
Default is FALSE (keep original column names).}

\item{significance_criteria}{List. Custom significance criteria for each method.
If NULL, uses standard criteria. Default is NULL.}

\item{min_cases}{Integer. Minimum number of cases required for inclusion.
Default is 0 (no minimum).}

\item{sort_by}{Character. Column name to sort results by. Default is "ror".}

\item{descending}{Logical. Sort in descending order? Default is TRUE.}

\item{max_rows}{Integer. Maximum number of rows to return. Default is NULL (all rows).}

\item{drop_na}{Logical. Remove rows with NA in term column? Default is TRUE.}

\item{remove_zero_cases}{Logical. Remove rows with zero cases? Default is TRUE.}
}
\value{
A data frame with formatted pharmacovigilance results. The structure
depends on the clear_format parameter:
\itemize{
\item If clear_format = FALSE: Returns selected columns with original names
\item If clear_format = TRUE: Returns standardized columns with clear names
}
}
\description{
This function processes raw pharmacovigilance signal detection results and
generates formatted tables suitable for reporting and analysis. It provides
flexible options for method selection, significance filtering, and output formatting.
}
\details{
The function supports the following signal detection methods with standard significance criteria:
\itemize{
\item \strong{ROR}: ROR > 2 & ROR_CI_low > 1
\item \strong{PRR}: PRR ≥ 2 & χ² ≥ 4 & cases ≥ 3
\item \strong{BCPNN (norm)}: IC > 0 & IC025 > 0
\item \strong{BCPNN (mcmc)}: IC > 0 & IC025 > 0
\item \strong{OE Ratio}: OE > 2 & OE_CI_low > 1
\item \strong{Odds Ratio}: OR > 2 & OR_CI_low > 1
\item \strong{EBGM}: EBGM > 2 & EBGM05 > 1
}
}
\examples{
\dontrun{
# Basic usage - all methods, original column names
basic_result <- format_pv_results(Immunity_signals)

# Remove rows where the case count is 0.
filtered_result <- format_pv_results(
  Immunity_signals,
  remove_zero_cases = TRUE
)

# Comprehensive filtering: zero cases, minimum case count, and significant signals.
comprehensive_result <- format_pv_results(
  Immunity_signals,
  remove_zero_cases = TRUE,
  min_cases = 3,
  show_significant_only = TRUE
)
}

}
