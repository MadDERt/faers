% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scan.R
\name{faers_phv_scan}
\alias{faers_phv_scan}
\alias{faers_phv_scan,FAERSascii-method}
\title{Whole-database pharmacovigilance signal scanning}
\usage{
faers_phv_scan(.object, ...)

\S4method{faers_phv_scan}{FAERSascii}(
  .object,
  .events = "pt",
  .drug_field = "drugname",
  .drug_pattern = NULL,
  .min_a = 3L,
  .methods = c("ror", "prr"),
  .chunk_size = 1000000L,
  .phv_signal_params = list(),
  BPPARAM = BiocParallel::SerialParam()
)
}
\arguments{
\item{.object}{A \link{FAERSascii} object, standardized with \link{faers_standardize}
and de-duplicated with \link{faers_dedup}.}

\item{...}{Unused arguments, included for S4 generic/method consistency.}

\item{.events}{A string, the event column of the standardized \code{reac} field.
Any MedDRA hierarchy column (\code{pt_name}, \code{hlt_name}, \code{hlgt_name},
\code{soc_name}, ...) added by \link{faers_standardize} can be used, as well as the
raw \code{pt} column. Defaults to \code{"pt"}.}

\item{.drug_field}{A string, the column of the standardized \code{drug} field
used to define drugs: \code{"drugname"} (default) or \code{"prod_ai"} (active
ingredient).}

\item{.drug_pattern}{An optional regular expression. If supplied, only drug
names matching it (case-insensitively) are kept in the result, enabling
scans of drug classes without paying for the whole database.}

\item{.min_a}{A single integer, the minimum number of reports co-listing a
drug-event pair for it to be kept. Defaults to \code{3L}.}

\item{.methods}{An atomic character, the disproportionality methods passed
to \link{phv_signal}. Defaults to \code{c("ror", "prr")}.}

\item{.chunk_size}{A single integer, the number of unique \code{primaryid}s
processed per chunk by the memory backend. Only relevant for
\code{database = "memory"}; smaller values trade speed for peak memory.}

\item{.phv_signal_params}{Other arguments passed to \link{phv_signal}.}

\item{BPPARAM}{A \link[BiocParallel:BiocParallelParam-class]{BiocParallel::BiocParallelParam} object.}
}
\value{
A \link[data.table:data.table]{data.table} with one row per kept
drug-event pair, sorted by the lower bound of the first method's confidence
interval in descending order: the drug column (named after \code{.drug_field}),
the event column (named after \code{.events}), the contingency table columns
\code{a}, \code{b}, \code{c}, \code{d}, and the columns of \link{phv_signal}.
}
\description{
\code{faers_phv_scan()} enumerates all \verb{drug x event} pairs present
in a standardized, de-duplicated \link{FAERSascii} object, builds the 2x2
contingency table for every pair in one aggregated pass, and runs
disproportionality analysis (\link{phv_signal}) on the whole table. In contrast
to \link{faers_phv_signal}, which answers "\emph{which} events are associated with
\emph{this} drug?", the scan answers "\emph{which} drug-event pairs stand out across
the whole database?", enabling hypothesis-generating pharmacovigilance.
}
\details{
The background distribution is the supplied object itself: for every pair
\verb{(drug, event)}, \code{a} is the number of distinct patients reported with both,
\code{n1.} the patients exposed to the drug, \code{n.1} the patients reporting the
event, and \code{n} the total number of distinct patients in \code{demo}.

Before counting, the drug side is normalized (\code{lower(trim(.))}) and empty,
\code{unknown} or \verb{?} names are dropped; on the event side \code{NA} and empty values
are dropped. Drug names are aggregated on their exact normalized value, so
brand-name variants are not merged (a mapping step may be added in the
future).

Pairs below \code{.min_a} are never evaluated, which is what keeps the output
and the expensive methods tractable. The default \code{.methods = c("ror", "prr")} are cheap vectorized methods suitable for scanning; \code{ebgm} and
\code{bcpnn_mcmc} are computationally expensive on large candidate tables and
trigger a warning when many pairs are supplied.

For whole-database scans (many years of FAERS quarterly data) the
\code{database = "duckdb"} backend of \code{\link[=faers]{faers()}} is strongly recommended: the
drug x event counting runs as a single out-of-core SQL aggregation. The
memory backend performs the equivalent computation with integer coding and
chunking over \code{primaryid} blocks (\code{.chunk_size}), which is exact because a
report never spans two chunks, so per-chunk distinct counts sum to the
database-wide counts.
}
\examples{
# the sample data below is standardized but not de-duplicated; real usage
# requires faers_standardize() + faers_dedup() before scanning
std_data <- readRDS(system.file("extdata", "standardized_data.rds",
    package = "faers"
))
std_data@deduplication <- TRUE
\dontrun{
# scan the bundled sample at the PT level
res <- faers_phv_scan(std_data, .events = "pt", .min_a = 1L)
head(res)

# for real analyses, run on many quarters with the duckdb backend
data <- faers(2015:2024, paste0("q", 1:4),
    dir = "faers_data", database = "duckdb"
)
data <- faers_dedup(faers_standardize(data, meddra_path))
res <- faers_phv_scan(data, .events = "pt", .min_a = 3L)
}
}
\seealso{
\link{phv_signal}, \link{faers_phv_signal}
}
